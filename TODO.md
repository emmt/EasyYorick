- Use global configuration file in, *e.g.*, `$PREFIX/etc/ypkg/config`).

- Have additional scripts in, *e.g.*, `$PREFIX/etc/ypkg/scripts.d`.

- Explain how to configure Emacs.

- Explain/check MiRA installation.
